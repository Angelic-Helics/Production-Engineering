global:
  resolve_timeout: 1m
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_from: 'teammaimuticahd@gmail.com'
  smtp_auth_username: 'teammaimuticahd@gmail.com'
  smtp_auth_password: 'cans bfdk ugfl gvvd'
  smtp_require_tls: true

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1m
  receiver: 'gmail'

receivers:
  - name: 'gmail'
    email_configs:
      - to: 'teammaimuticahd@gmail.com'
        send_resolved: true
        tls_config:
          insecure_skip_verify: true
